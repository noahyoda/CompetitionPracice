using System;

namespace contestStruggles
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            solvedAvg = y / solvedNum
            totalAvg = (x + solvedAvg) / totalNum
            70 = (x + 50) / 2
            2 * 70 = x + 50
            X = (2 * 70) - 50
            */
            /*
                        bool exitCon = false;
                        while(!exitcon){
                            Console.WriteLine("give 4 input:");
                            Console.WriteLine(calc());
                            string x = Console.readLine():
                            if(x == "exit")
                                exitCon = true;
                        }

                    }

                    public static int calc(){

                        int totalNum = Console.read();
                        int solvedNum = Console.read();
                        int totalAvg = Console.read();
                        int solvedAvg = Console.read();
                        int remainAvg = (totalNum * totalAvg) - solvedAvg;
                        return remainAvg;
                    }
            */
        }
    }
}
